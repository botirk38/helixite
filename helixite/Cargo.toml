[package]
name = "helixite"
version = "0.1.0"
edition = "2024"
description = "Embedded graph-vector database for Rust"
license = "MIT"

[dependencies]
heed = "0.21"
serde = { version = "1", features = ["derive"] }
bincode = "1"
thiserror = "2"
rand = "0.9"

[dev-dependencies]
tempfile = "3"
